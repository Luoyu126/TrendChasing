name: Temporary Runner Probe
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify hosted Docker runner
        run: |
          docker version --format '{{.Server.Version}}'
          docker compose version
          python3 --version
